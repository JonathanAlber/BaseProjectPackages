namespace Base.AttributePackage
{
    /// <summary>How tall an inspector button is drawn.</summary>
    public enum EButtonSize : byte
    {
        /// <summary>One line, the same height as a field.</summary>
        Normal = 0,

        /// <summary>Half again as tall, for the one action a component is mostly about.</summary>
        Large = 1
    }
}